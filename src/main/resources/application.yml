spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://svc.sel3.cloudtype.app:31038/testUnity
    username: 'root'
    password: '1234'
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  logging:
    level:
      org:
        hibernate:
          type:
            descriptor:
              sql: trace


# JWT
jwt:
  secret: YOUR_SECRET_KEY
  access-expired: 1800

